import UIKit

extension UIImage {

    internal enum Icons {
        
        public static let back          = UIImage(named: "") ?? UIImage()
        
    }
    
    internal enum Images {
        
        public static let background    = UIImage(named: "") ?? UIImage()
        
    }
}
